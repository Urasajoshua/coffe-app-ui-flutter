import 'package:flutter/material.dart';

class SingleItemScreen extends StatelessWidget {
  SingleItemScreen(img, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
